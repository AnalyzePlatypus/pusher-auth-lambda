{
  "name": "pusher_auth_lambda",
  "version": "1.0.0",
  "description": "Lambda for signing tokens for Pusher",
  "main": "index.js",
  "scripts": {
    "invoke": "node ./dev_scripts/invoke.js",
    "build": "./dev_scripts/build.sh",
    "upload": "./dev_scripts/upload.sh",
    "deploy": "npm run build && npm run upload"
  },
  "author": "Michoel Samuels",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "pusher": "^3.0.0"
  }
}
